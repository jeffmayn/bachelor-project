TODO.txt
1 Kims tests der fejler:

	O_ArrayOfRecords - segfault! alvorligt skal kigges på.

	O_ArrayComparisonsB - segfault! alvorligt skal kigges på.

	R_ErrRuntimeNullPointer - segfault! alvorligt skal kigges på. måske noget med at kontrollere for null flere steder end vi gør? ved ikke om vi skal kontrollere dette.

	O_TypeJumpScope - Fejl i typecheck, skal fikses, problem med allokering

	O_MultiDimArray - fejl i type checker, igen noget med arrays og allokering.

	O_FuncRedefinedInItself - måske en fejl med et label der ikke bliver talt op eller skabt korrekt.

herfra og ned melder testen ikke "correct output" men det er ok.

	C_ReturnInMainScope - skal gerne returnere 42, det bliver læst som en errorcode, ikke sikker på at dette er korrekt. - spørg kim

	F_FuncParamsEvalOrder - fejler fordi vi evaluere funktions parametre fra højre til venstre, ville være cool at ændre.

	F_SimpleStructuralEquiv - fejler fordi vi ikke understøtter structure equivilence.

	F_RecordIsTupleOrSet - Fejler fordi vi har implementeret records som tupler og ikke som set, det er ok at den fejler.	

	C_ErrTypeLoop - det er meningen den skal fejle, ser us som om den fejler af den rigtige grund. måske lige kig på prints.

	C_ErrUnmatchedBeginComment - det er meningen den skal fejle, ser us som om den fejler af den rigtige grund. måske lige kig på prints.

	C_ErrInvalidToken - det er meningen den skal fejle, ser us som om den fejler af den rigtige grund. måske lige kig på prints.

	R_ErrRuntimeDiv0 - testen fejler med vilje og den returnere korrekte fejlkode 3, alt er godt.

	R_ErrOutOfBounds1 - testen fejler med vilje og den returnere korrekte fejlkode 2, alt er godt.

	R_ErrOutOfBounds2 - det er meningen den skal fejle, ser us som om den fejler af den rigtige grund, errorcode 2 bliver returneret som ventet.

	R_ErrRuntimeNegArraySize - det er meningen den skal fejle, ser us som om den fejler af den rigtige grund, errorcode 4 bliver returneret som ventet.

	R_ErrRuntimeOutOfMem - testen fejler med vilje og den returnere korrekte fejlkode 6, alt er godt.

	C_ErrFuncParamsTooMany - det er meningen den skal fejle, ser us som om den fejler af den rigtige grund. måske lige kig på prints.

	C_ErrFuncParamsTooFre - det er meningen den skal fejle, ser us som om den fejler af den rigtige grund.

	C_ErrFuncParamsInvalidType - det er meningen den skal fejle, ser us som om den fejler af den rigtige grund.

	C_NullWrong - det er meningen den skal fejle, ser us som om den fejler af den rigtige grund, måske lige ryd op i prints.

	C_ErrAssignToType - det er meningen den skal fejle, ser us som om den fejler af den rigtige grund.

2 Ryd op i mærkelige prints!

