#Erklær en funktion der tager som parameter et anonymt array indeholdende type der ikke findes (FORBUDT)
#Kald funktion med parameteren
func f(a:array of X):int
  write 7;
  return 8;
end f
var y:int;
var b:array of X;
y = f(b);

write 9;
