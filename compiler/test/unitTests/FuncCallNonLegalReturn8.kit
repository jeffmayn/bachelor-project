(*test af funktion
  ukorrekt kitty program
  fejl i funktionskald, parametre stemmer ikke
  overens med definition*)

  func retAnonArray (x : array of array of int) : array of array of int

	var v : array of array of int;
	var i : int, j : int;
	i = 0;
	allocate v of length 5;
	while (i < 5) do {
		allocate v[i] of length 5;
		j = 0;
		while(j<5) do {
			v[i][j] = x[4-i][4-j];
			j = j + 1;
		}
		i = i + 1;
	}
	return v;

end retAnonArray

var q : array of array of bool;
var t : array of array of int;
var i : int, j : int;

allocate t of length 5;
i = 0;
while (i < 5) do {
	allocate q[i] of length 5;
	j = 0;
	while(j<5) do {
		q[i][j] = j + i;
		j = j + 1;
	}
	i = i + 1;
}

(*q er array of bool ikke array of int*)
q = retAnonArray(t);

i = 0;
while (i < 5) do {
	j = 0;
	while(j<5) do {
		write t[i][j];
		j = j + 1;
	}
	i = i + 1;
}