(*test af funktion
  funktionen tager of returnere simple typer
  korrekt kitty program*)

func retInt (x : int) : int

	x = x + 1;
	return x;

end retInt

func retBool (boolean : bool) : bool

	boolean = true;
	return boolean;

end retBool

write retBool(false);
write retInt(1);