#Erklær en funktion der returnerer et anonymt array indeholdende en type, der ikke findes (FORBUDT)
#Forsøg at returner et array af typen
func f():array of X
  var a: array of int;
  allocate a of length 8;
  write 7;
  return a;
end f

write 9;
