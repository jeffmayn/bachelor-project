(* korrekt kitty-program, her testes for, at weeder
   finder return-statements i en if-then-else betingelse.
   Programmet skal returnere 0 *)

var x:bool;
var z:int;
var u:int;

func f2(x:bool,u:int) : bool
  if(x)
  then{
    u = 9;
    return x;
  }
  else{
    u = 7;
    return x;
  }
end f2

x = true;
