(* ukorrekt kitty-program, her testes for, at weeder
   kan håndtere mangel på return-statement i then- og
   else delen af en nested if-then-else betingelse, når
   der findes en return-statement uden for denne betingelse.
   Programmet skal returnere 0 *)

var x:bool;
var z:int;
var u:int;
var y:bool;

func f2(x:bool,u:int,y:bool) : bool
  if(x)
  then{
    if(y)
    then{
      u = 10;
      #return x;
    }
    else {
      u = 9;
      #return x;
    }
    return x;
  }
  else{
    u = 7;
    return x;
  }
end f2

x = false;
