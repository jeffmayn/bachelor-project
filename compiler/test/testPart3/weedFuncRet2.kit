(* ukorrekt kitty-program, her testes for, at weeder
   finder mangel på return-statement i en if-then betingelse.
   Programmet skal returnere -1 *)

var x:int;

func g(x:bool,u:int):bool
var w:bool;
if(x) then u = 7;
#return true;
end g

x = 2;
y = z;
