(* korrekt kitty-program, her testes for, at
   en liste af typen int ikke må tildeles en bool.
   Programmet skal returnere 0 *)

type A = int;
type B = A;
type INTS = array of int;

var arr1:INTS;

var x:A, a:B, c:bool;
c=true;
x=5;
a = arr1[true];
