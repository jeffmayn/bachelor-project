(* korrekt kitty-program, her testes for, at
   der der kan tildeles et heltal til en bruger
   defineret integer gennem flere lag.
   Programmet skal returnere 0 *)

type A = int;
type B = A;

var c:B;
c=4;
