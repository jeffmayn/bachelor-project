(* korrekt kitty-program, her testes for, at weeder
   finder en utilgængelig variabel i en if-then-else betingelse.
   Programmet skal returnere 0 med en warning *)

var x:bool;
var z:int;
var u:int;

func f2(x:bool,u:int) : bool
  if(x)
  then{
    return x;
    u = 9;
  }
  else{
    u = 7;
    return x;
  }
end f2

x = true;
