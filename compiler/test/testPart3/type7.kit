(* korrekt kitty-program, her testes for, at
   lister kan initialiseres og tildeles et heltal.
   Programmet skal returnere 0 *)

type A = int;
type B = A;
type INTS = array of int;

var arr1:INTS;

var x:A, a:B, c:bool;
c=true;
x=5;
a = arr1[x];
