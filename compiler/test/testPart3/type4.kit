(* ukorrekt kitty-program, her testes for, at
   der der kan tildeles et null til en bruger
   defineret integer gennem flere lag.
   Programmet skal returnere -1 *)

type A = int;
type B = A;

var c:B;
c=null;
