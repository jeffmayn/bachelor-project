(* korrekt kitty-program, her testes for, at der kan
   erklæres en record som indeholder en type der
   først erklæres senere. Programmet skal returnere 0 *)

type R = record of {x:int};
type K = record of {y:R};
#type X = X;
var u:R;
var i:K;
u.x = 3;
u.x = i.y.x;
