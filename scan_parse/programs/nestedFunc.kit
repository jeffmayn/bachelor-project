func FUNC(x: int, y: array of int) : bool
  func empty() : myType
    var x : int;
    func add(l:List, n:ListNode) : int
      if(list.head == null)
      then{
        list.head = n;
        list.tail = n;
        empty = false;
      }
      else{
        n.next = list.head;
        n.next.prev = n;
        n.prev = null;
        l.head = n;
      }
      return 0;
    end add
    x = 42;
  end empty
  return x == y[12];
end FUNC


x = 3;
